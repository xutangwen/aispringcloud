server:
  port: 8761
eureka:
  client:
    register-with-eureka: false  # 是否将自己在注册中心注册   不注册自己，只注册别人
    fetch-registry: false # 是否同步其他注册中心的数据
    service-url:
      defaultZone: http://localhost:8761/eureka/ # 注册中心的访问地址